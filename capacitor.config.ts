import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alex.currencyexchange',
  appName: 'Currency Exchange',
  webDir: 'dist'
};

export default config;
